---
const { title, info, symbollink} = Astro.props
---

<div class="w-[80vw] mx-auto my-4 flex-1 border-2 border-solid p-3 border-red-800 md:w-1/3 bg-white rounded-md shadow-xl">

	<h1 class="text-center text-4xl mb-2">{title}</h1>
	<img src={symbollink} alt={title + " symbol"} class="w-[200px] h-[200px] mx-auto">
	<h2 class="text-2xl">{info}</h2>
</div>

